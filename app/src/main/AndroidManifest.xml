<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.madspild">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/madspild_icon"
        android:label="@string/appname"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GS1">
        <!-- android:theme="@style/AppTheme"> -->

        <activity
            android:name="madspild.Activities.StartActivity"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/Theme.GS1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="madspild.Activities.MainActivity"/>
        <activity android:name="madspild.Activities.EditProfileActivity"/>

    </application>

</manifest>